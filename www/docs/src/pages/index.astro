---
import { getEntry } from "astro:content";
import MainLayout from "../layouts/MainLayout.astro";

const post = await getEntry("docs", "en");

const { Content, headings } = await post.render();
---

<MainLayout headings={headings} {...post.data}>
  <Content />
</MainLayout>
